const Discord = require('discord.js'),
      moment = require('moment'),
      fn = require('/app/util/fn')
      // db = require('quick.db')

// const data = new db.table("data")

module.exports = {
  name: "bugreport",
  execute: async (client, msg, args, shared) => {
		
  }
}