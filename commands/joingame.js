const Discord = require('discord.js')

module.exports = {
  name: "joingame",
  aliases: ["join","jg"],
  run: async (client, message, args, shared) => {
    let gamemode = args[0].toLowerCase()
    if (["quick"])
  }
}