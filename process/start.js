const Discord = require("discord.js"),
      moment = require("moment"),
      db = require("quick.db")

const games = new db.table("Games")

const broadcast = async (client, mode = "quick", gameID) => {
  
}

module.export = async (client, gameID) => {
  
}